# Todos

*2026-08-04*

* [x] Failed CLI operations should not change the server's state.

  e.g. Right now, if I upload a template that has missing dependencies then I'll
  get a "missing dependency" error, but the template will also have been created
  on the server.

* [x] Test the damn thing!

  There *must* be bugs lurking. I don't want to spend my time debugging when I
  just want to publish an article.

  Use Hedgehog state machine testing to find issues.

* [x] Embed resources such as CSS and JavaScript into templates via template parameters.

  e.g.

  ```
  <script type="text/javascript" src={{resource.js.my-script.url}}
  ```

  These must also be registered as dependencies of the template.

  If the resource is missing, then the error message should point to part of
  the template:

  ```
  (template:my-template): error: no such resource
    |
  X | <script type="text/javascript" src="{{resource.js.my-script.url}}"
    |                                                ^
  ```

  * [x] Provide resources as template parameter
  * [x] Register resources as template dependencies
  * [x] Implement user-defined properties (i.e. for `url`)
  * [ ] ~~Use URL property to perform routing~~
    * [ ] ~~Article to HTML rule propagates article's `metadata.url` to html's `url`~~
    * [ ] ~~When the `url` property is set, the resource is added to a routing table~~

      The routing table should live in memory. But how will it be rebuilt on
      restart?

      Maybe instead of a `url` property there's a resource type for routes, and
      new entries are added to that. On startup, all resources are read in to
      create the table, and when a new route resource is added, the in-memory
      version is updated.

  * [x] Use a routing table to serve resources.
    * [x] Create `route` resource type
    * [x] Define `route` resource content format (something like `url -> type:name`)
    * [x] Create in-memory routing structure
    * [x] Use in-memory router to serve resources
    * [x] Init router from `route` resources on startup

    * [x] Create `route` entries for `html` generated by the article-to-HTML rule

      * [x] Remember to update the in-memory representation, too.

  * [ ] Rebuild dependents when property changes
  * [ ] Clean up error messages

* [x] Handle the trailing newline that Helix adds to `config:base-url`

  Resolution: if a resource has property `content-format = "line"`, then trailing
  newlines are stripped when the content is passed to a template.

  Brainstorm:

  * Strip in template (I don't like this, have to remember every time)
  * Strip unconditionally (don't like this either, prefer WYSIWYG)
    * But if I introduce a new mime type like `text/x.line`, I can strip the
      trailing newline from the input and add it back to the output.
  * Use TOML syntax for config, quote the string: `"https://localhost:8000"`
  * Store many such settings as a TOML document, i.e.

    ```toml
    base-url = "https://localhost:8000"
    setting = "..."
    another-setting = "..."
    ```

  * Store `base-url` as a property of some resource.
    then? Something like `config:server`?

  * Add a `format` property to `config`.

    * `format = "text"` means use the config's content verbatim.
    * `format = "line"` means to split the content into lines and use the first line.

* [x] Strip blog-metadata code blocks before rendering markdown to HTML.

* [x] Handle recursive build dependencies.

  `page.html.temple` includes navigation:

  ```
  <nav>
  <ol>
    <li><a href="{{resource.html.index.url}}">Home</a>
    <li><a href="{{resource.html.about.url}}">About</a>
    <li><a href="{{resource.html.resources.url}}">Resources</a>
  </ol>
  </nav>
  ```

  Which means that transitively, `article.html.temple` depends on specific html
  resources. But we already have `html:article-{name}` depending on
  `article.html.temple` via the article rendering rule. This seems fraught.
  Maybe the dependency graph is still acyclic because `article-render`'s
  outputs don't overlap with `page.html.temple`'s inputs.

  Another loop is that the `index`, `about`, and `resources` will be compiled
  to `html` using some other render rule that involves `page.html.temple`.
  So the resulting html depends on `page.html.temple`, and `page.html.temple`
  also depends on the html.

  I could break the cycle by adding a `page` resource that has `url` in its
  metadata and reference that instead:

  ```
  <nav>
  <ol>
    <li><a href="{{resource.page.index.metadata.url}}">Home</a>
    <li><a href="{{resource.page.about.metadata.url}}">About</a>
    <li><a href="{{resource.page.resources.metadata.url}}">Resources</a>
  </ol>
  </nav>
  ```

  Then the page render rule ensures that the generated html has that URL.

  If I keep managing to break cycles, then it should be an error to construct
  cyclic rules.

  Update: deferring rules with the above `page` resource fixes this for me. In
  a deferred transaction, I can create `template:base.html.temple`,
  `page:about`, and `page:resources`. The `page` resources don't depend on the
  template; only their rendering rule does. When the deferred transaction is
  committed, a rule for `template:base.html.temple` checks that the references
  `page` resources exist, and that passes. Then the `page` resources are
  rendered to HTML using `template:base.html.temple` and placed at the URL in
  their metadata.

* [x] Support footnotes

  The footnotes extension from `commonmark-extensions` is broken:
  <https://github.com/jgm/commonmark-hs/issues/43>

* [ ] Get rid of duplicate change accumulation

  `asChanges` in `Blog.Build` should be some kind of set, not a list.
  That way, creating a resource then setting its property will only register a
  single change.

* [ ] Ensure that template dependencies are kept in sync.

  In my test directory, I see that `article.html.temple` has a dependency on
  `text:hello`, even though that's not true. I was true in the past, though,
  so it seems like old dependencies aren't being cleaned up.

* [ ] Figure out resource deletion.

* [ ] Consider using properties instead of an adjacency resource.

  The article adjacency rule could set `previous` and `next` properties on
  the respective article, which would mark the article as "changed".

* [ ] Store metadata as a property

  Right now metadata is a special case, but everything about the way it's handled
  points to treating it as a property.

* [ ] Declare properties in resource type

  The metadata's types are declared in the resource type, but if metadata is just
  a property, then it makes sense to have all properties declared and typed in the
  resource type.

* [ ] Improve incrementality

  * If I edit the content of a CSS file, then I shouldn't have to rebuild something
    that depends only on the CSS file's URL.

* [ ] Require documentation for resource definitions

  i.e.

  ```toml
  content-type = "..."

  description = "This is why the thing exists"
  ```

* [ ] Improve footnote HTML

  The Pandoc AST doesn't let me assign an ID to an `<li>` tag, so I have to
  wrap each footnote in a `<div>` with a tag. I want the `<li>`s to have IDs
  because the markup is cleaner.

* [ ] Store dependencies/dependents as a normal metadata value.

  I added a special case to import/export to handle these because they're
  directories but all other properties are files.
